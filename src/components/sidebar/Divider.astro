<div class="relative w-full py-4 text-center text-sm font-semibold lowercase">
  <span class="px-2 pt-2 capitalize"><slot /></span>
  <div class="absolute bottom-6 flex w-full justify-between">
    {
      [...Array(2)].map((_) => (
        <div class="h-1 w-2/6 border-t-2  border-primary" />
      ))
    }
  </div>
</div>
